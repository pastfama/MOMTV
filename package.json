{
  "name": "momtv",
  "version": "0.1.0",
  "private": true,
  "description": "MOMTV - AI-Powered Cartoon TV Studio",
  "scripts": {
    "studio": "pnpm --filter @momtv/studio dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.18.0"
  }
}